# 🍕 Pizza Store Test Task 
Django + PostgreSQL on Heroku:

https://shtreis-pizza.herokuapp.com/

To run project locally with sqlite database:
```commandline
pip install -r requirements.txt

python manage.py runserver
```

